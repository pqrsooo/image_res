name: image_res
description: A Flutter package that organizes image's resolution variants inside your asset's folder.
version: 0.0.3
author: Parinthorn Saithong <parinthorn.sa@gmail.com>
homepage: https://github.com/pirsquareff/image_res

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^2.1.15
  path: ^1.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

executables:
  imgres: main

flutter:
