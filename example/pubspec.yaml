name: image_res_example
description: An example package for testing
homepage: https://github.com/pirsquareff/image_res

version: 1.0.0

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  image_res:
    path: ../
  
flutter:
